# 踩坑记录(问题与经验库)

> 项目跑动中遇到的问题、解决办法、死路、踩坑经验。**append,不删改**。
> 与 `decisions.md` 的区别:这里记"什么坏了、怎么修";`decisions.md` 记"我们决定怎么做"。
> 某条提炼出长期「禁区 / 约定」时 → 把那句精简结论同步进 `AGENTS.md`,详情留这里。

<!-- 每条一个块,新的放最上面:

## YYYY-MM-DD · <问题标题>   [已解决 / 未解决]
- **现象**:出了什么问题 / 报错 / 卡点
- **触发条件**:什么情况下会出现
- **试过没用的**(关键!记死路,免得重走):
  - 方案 A —— 为什么不行
  - 方案 B —— 为什么不行
- **根因**:
- **解决**:最终怎么解的(给指针:相关文件 / commit / docs 路径)
- **教训 / 怎么避免**:(若升级成长期规则 → 同步一句进 AGENTS.md 禁区)
-->

## 2026-09-12 · 移动后端后更新容器路径
- 文件夹移动后，旧容器仍记录原源码绝对路径；需用新位置的 compose 配置重新创建服务。
- 保留 priceagent-local 项目名和原数据卷，避免误建空数据库；前端开发服务在新目录验证后关闭。

## 2026-09-12 · 嵌套 Git 会仅提交引用
- backend 带有自己的 .git 时，根提交记录的是模式 160000 的 gitlink，不能代表后端源码已包含。
- 本次备份并移出嵌套 .git，解除旧 gitlink；后端成为根仓库普通目录。根提交前应核对文件清单和忽略规则。
