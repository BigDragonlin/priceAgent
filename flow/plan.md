# 当前任务：根 Git 统一管理前后端

用户要求取消 backend 独立 Git，并纳入本地测试密码配置。

1. 备份后端 Git 元数据和根 Git 索引，再将 backend 外部仓库引用转换为普通源码目录。
2. 按用户明确确认，纳入本地 compose、运行说明、Maven 配置、初始化脚本和真实 API 密钥文件；不纳入日志、备份、编译产物。
3. 核对源码完整性、忽略规则及独立评审，更新说明，不自动提交业务改动。

以下为此前计划记录。

# 当前任务：前后端收进 priceAgent

用户已明确要求 priceAgent 内包含 frontend 和 backend；保留全部既有代码、未提交文件及数据库。

1. [已完成] 前端源码与运行配置移入 frontend；根级 Git、协作配置、flow 和 docs 留在根目录。
2. [已完成] 相邻后端整体移入 backend，保留其 Git 历史和 .local 本地配置。
3. [已完成] 更新目录说明和启动路径，恢复原后端服务，用 ego-browser 验证页面连接。
4. [已完成] 完成独立评审，关闭本次临时前端服务，记录结果；不提交、不部署。

以下保留原报价 Demo 计划，本次不执行其中的业务开发。

# 报价 Demo 实施计划

依据本轮已确认架构与用户“允许”继续制作的授权执行；保留 `需求文档.md` 旧改动。

- [ ] 后端：读取总 Skill 和子 Skill，提供示例产品/成本/规则接口。
- [ ] 后端：拆分成本、运输、利润和税费计算单元；校验参数和硬性约束，保留计算快照。
- [ ] 参数建议：读取 Skill 与业务资料，仅建议目标利润等允许参数；用户确认后用于本次报价。使用用户指定的 DeepSeek deepseek-flash 真实接口，失败时不回退模拟。
- [ ] 前端：需求表单、建议对比、计算明细、客户版和内部版 Excel 下载。
- [ ] 验证：后端计算与导出测试、前端类型/构建检查、ego-browser 真实交互及下载、不同模型独立评审。
- [ ] 交付：记录接口、运行方法、限制和验收结果；关闭本次启动的服务，不停止原先运行的服务。

实现者 Codex；验收标准和产出见 `flow/tasks/T01-报价Demo.md`。不提交、不部署，不做 CI/CD。
